<html>
    <head><title><%= @user.name %></title></head>
    <body>
        <div class = 'container'>
            <div class = 'personal'>
              <%= @user.name %>
              <div class = 'photo'>
                <%= form_for @user, :url => users_path, :html => { :multipart => true } do |form| %>
                    <%= form.file_field :avatar %>
                <% end %>
                <%= image_tag @user.avatar.url %>
              </div>
            </div>
            <div class = 'latest_photos'>
              <% if Photo.count.zero? %>
                <p>add your first photo. HAY QUE PONER UN "EXAMINAR" para seleccionar la foto. Despues añadir al usuario esa
                 foto y mostrarla en su pagina principal.</p>
                <%= link_to 'Add Photo' , new_photo_path %>
              <% else %>
                  <%= image_tag @user.photo  %>
                  <%= button_to 'Add Photo' , home_add_path(user_id: @user) %>
              <% end %>
            </div>

            <div class = 'calendar'></div>
            <div class = 'table'></div>
        </div>

    </body>

</html>


